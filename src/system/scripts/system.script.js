#!/usr/bin/env node
/* eslint no-console: 0 */

const program = require('commander');

program
  .version('0.0.1')
  .description('GoPool Command line interface')
  .usage('[Command] [args] [Options]');
